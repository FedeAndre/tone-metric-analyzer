name: TMA gait incremental exact validation

on:
  push:
    branches:
      - tma-gait-incremental-exact-20260924
    paths:
      - 'research/tma_gait_incremental_exact.py'
      - '.github/workflows/tma-gait-incremental-exact.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install analysis dependencies
        run: python -m pip install --upgrade pip && pip install 'numpy>=2,<3' 'pandas>=2,<3' 'scipy>=1.14,<2' 'requests>=2.32,<3' 'scikit-learn>=1.5,<2'
      - name: Run exact-engine incremental information validation
        id: analysis
        continue-on-error: true
        shell: bash
        run: |
          mkdir -p research/tma_gait_incremental_exact_results
          set +e
          PYTHONPATH=.:research python research/tma_gait_incremental_exact.py 2>&1 | tee research/tma_gait_incremental_exact_results/run.log
          code=${PIPESTATUS[0]}
          echo "$code" > research/tma_gait_incremental_exact_results/exit_code.txt
          exit "$code"
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tma-gait-incremental-exact-results
          path: research/tma_gait_incremental_exact_results/
          if-no-files-found: error
      - name: Commit results
        if: always()
        run: |
          git config user.name 'TMA analysis bot'
          git config user.email 'actions@users.noreply.github.com'
          git add research/tma_gait_incremental_exact_results
          if ! git diff --cached --quiet; then
            git commit -m 'Add exact-engine incremental gait validation results/log'
            git push origin HEAD:tma-gait-incremental-exact-20260924
          fi
      - name: Fail if analysis failed
        if: always()
        run: |
          code=$(cat research/tma_gait_incremental_exact_results/exit_code.txt)
          exit "$code"
