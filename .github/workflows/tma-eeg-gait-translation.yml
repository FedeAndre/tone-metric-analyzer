name: TMA EEG-gait translational transform

on:
  push:
    branches: [tma-eeg-gait-translation-20260926]
    paths:
      - 'research/tma_eeg_gait_translation.py'
      - 'research/tma_eeg_gait_multifrontend.py'
      - '.github/workflows/tma-eeg-gait-translation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install 'numpy>=2,<3' 'pandas>=2,<3' 'scipy>=1.14,<2' 'requests>=2.32,<3'
      - name: Run multi-front-end EEG-gait TMA
        run: PYTHONPATH=.:research python research/tma_eeg_gait_multifrontend.py
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: tma-eeg-gait-multifrontend-results
          path: research/tma_eeg_gait_multifrontend_results/
          if-no-files-found: error
      - name: Commit results
        run: |
          git config user.name 'TMA analysis bot'
          git config user.email 'actions@users.noreply.github.com'
          git add research/tma_eeg_gait_multifrontend_results/
          if ! git diff --cached --quiet; then
            git commit -m 'Add multi-front-end EEG-gait TMA results'
            git pull --rebase origin tma-eeg-gait-translation-20260926
            git push origin HEAD:tma-eeg-gait-translation-20260926
          fi
