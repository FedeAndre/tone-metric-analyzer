name: TMA gait fast exact validation

on:
  push:
    branches:
      - tma-gait-gauge-validation-20260923
    paths:
      - 'research/tma_gait_fast_exact.py'
      - '.github/workflows/tma-gait-fast-exact.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install analysis dependencies
        run: python -m pip install --upgrade pip && pip install 'numpy>=2,<3' 'pandas>=2,<3' 'scipy>=1.14,<2' 'requests>=2.32,<3'
      - name: Run exact-equivalent gait validation
        run: PYTHONPATH=.:research python research/tma_gait_fast_exact.py
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: tma-gait-fast-exact-results
          path: research/tma_gait_gauge_results/
          if-no-files-found: error
      - name: Commit results
        run: |
          git config user.name 'TMA analysis bot'
          git config user.email 'actions@users.noreply.github.com'
          git add research/tma_gait_gauge_results
          if ! git diff --cached --quiet; then
            git commit -m 'Add exact-equivalent gait gauge validation results'
            git push origin HEAD:tma-gait-gauge-validation-20260923
          fi
